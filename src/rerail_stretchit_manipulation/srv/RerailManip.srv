# Define the request part (what the client sends)
geometry_msgs/Pose target_pose

---
# Define the response part (what the service sends back)
bool success
string message  # You can add a message to give more information about the result
